1. ДҚБЖ ядросы жауап береді
A) сыртқы жадтағы деректерді басқару
B) ДБ тұтастығын жаһандық тексеру
C) ДБ жүктеу және түсіру
D) жедел жады буферлерін басқару
E) статистиканы жинау
F) дерекқордың тұтастығын жергілікті тексеру
G) ДБ тілінің операторларын кейбір орындалатын бағдарламаға құрастыру
H) транзакцияларды басқару және журналдандыру

 2. Eң аз күнді көрсететін жолдарды таңдау
A) SELECT * FROM deliverynotes WHERE dndate < '01/01/2017';
B) SELECT goodslist.*, (SizeX*SizeY*SizeZ) AS volume FROM goodslist
WHERE amount>0 AND (grossweight>100 OR
SizeX*SizeY*SizeZ>1000000000);
C) SELECT * FROM goodslist WHERE SizeX<=1000 AND SizeY<=1000 AND
SizeZ<=1000;
D) SELECT * FROM deliverynotes WHERE dndate>='01/01/2017';
E) SELECT supplier FROM suppliers WHERE supplier <'П';
F) SELECT * FROM deliverynotes WHERE dndate BETWEEN '2018/01/01'
AND '2018/12/31';

 3. Жеке нысан қасиеті
A) бір мәнді атрибут
B) көп мәнді атрибут
C) қарапайым атрибут
D) анықталмаған атрибут
E) бірдей мәнді атрибут
F) көпполярлы атрибут

 4. Tранзакция қасиеттері
A) тұтастық
B) икемділік
C) ашықтық
D) атомарлық
E) бөліну
F) серия бойынша
G) қауіпсіздік

5. Pеляциялық модель деректердің тұтастығын қамтамасыз ету үшін сыртқы
кілттерге шектеу қояды
A) домендік
B) жоғарғы
C) семантикалық
D) ішкі
E) сілтемелік
F) сыртқы

 6. ER моделінде ерекшеленетін негізгі компоненттер
A) мәндер, байланыстар, атрибуттар
B) объектілер, байланыстар, атрибуттар
C) мәндер, байланыстар, ағаш құрылымдары
D) кілт-мәні, объектілер, құжаттар
E) құжаттар, мәндер, атрибуттар
F) байланыстар, атрибуттар, ағаш құрылымдары

 7. Tапсырыс берілмеген барлық өнімдер
A) SELECT * FROM products WHERE productId NOT IN (SELECT DISTINCT
productId FROM orders);
B) SELECT * FROM products WHERE product_id IN (SELECT product_id
FROM orders);
C) SELECT * FROM products WHERE product_id NOT EXISTS (SELECT
DISTINCT product_id FROM orders);
D) SELECT * FROM product WHERE product_id NOT IN (SELECT DISTINCT
product_id FROM order);
E) SELECT * FROM products WHERE product_id NOT IN (SELECT product_id
FROM orders GROUP BY product_id);
F) SELECT * FROM products WHERE productId NOT EXISTS (SELECT
productId FROM orders);
G) SELECT * FROM products WHERE product_id NOT IN (SELECT
DISTINCT product_id FROM orders);
H) SELECT * FROM products WHERE product_id NOT IN (SELECT product_id
FROM orders HAVING COUNT(*) > 0);

 8. Pеляциялық модельдегі қатынас дәрежесі
A) өрістердің саны
B) сыртқы кілт
C) бастапқы кілт
D) кортеж
E) атрибут
F) бағандардың саны
G) кезек саны
H) домен

 9. Геотаратылған мәліметтер базасын басқару жүйесі
A) Google Spanner
B) Microsoft SQL Server
C) PostgreSQL
D) Oracle
E) db4o
F) Cassandra
G) MySQL
H) Amazon DynamoDB

10. Қатынастан тізімде көрсетілген атрибуттарды таңдауға мүмкіндік береді
A) байланыс
B) бөлу
C) проекция
D) композиция
E) ауыстыру
F) шектеу

